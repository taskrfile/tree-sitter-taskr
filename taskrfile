env dev:
  file = test.env

task generate:
  desc = generate parser
  run  = tree-sitter generate

task test:
  desc  = tests the parser
  run   = tree-sitter parse taskrfile
  alias = t
  needs = generate

task highlight:
  run   = tree-sitter highlight taskrfile
  alias = h

task build:
  run   = tree-sitter build
  alias = b
